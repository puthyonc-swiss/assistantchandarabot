# Telegram bot library (current stable as of June 2026)
# [webhooks] extra is REQUIRED because bot.py uses run_webhook() - without
# this extra, Render's deploy fails with:
#   RuntimeError: To use `start_webhook`, PTB must be installed via
#   `pip install "python-telegram-bot[webhooks]"`.
python-telegram-bot[webhooks]==22.7

# Google Gemini AI SDK (current official unified SDK)
google-genai==2.3.0

# Used to load environment variables from a local .env file when testing
# on your own computer (Render itself doesn't need this, it has its own
# environment variable system)
python-dotenv==1.0.1
