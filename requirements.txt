# Telegram bot library (current stable as of June 2026)
python-telegram-bot==22.7

# Google Gemini AI SDK (current official unified SDK)
google-genai==2.3.0

# Used to load environment variables from a local .env file when testing
# on your own computer (Render itself doesn't need this, it has its own
# environment variable system)
python-dotenv==1.0.1

# NEW: needed for the custom webhook server in bot.py's main() function.
# We switched from application.run_webhook() (which only exposes the
# Telegram webhook path) to PTB's documented "custom webhook" pattern,
# so we could add a second "/health" route for cron-job.org to ping and
# keep this free Render instance awake. starlette builds the two routes
# (Telegram webhook + health check), uvicorn runs the actual server.
starlette==0.41.3
uvicorn==0.34.0
